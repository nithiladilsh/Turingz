# Module 3 — Build Evidence Log

Per-step record of what was built, the design evidence (citation / reason) behind
each decision, and the verification proof. One folder per step. Artifacts produced
with stand-in solvers are labelled *harness validation*; they are regenerated with
the real FNO / spectral solvers at integration.

| Step | What | Design evidence | Proof | Status |
|------|------|-----------------|-------|--------|
| 1 | Ground-truth loader + scoring metric (`groundtruth.py`) | Metric matches `hybrid_pde/common/evaluate.py`; `.pt` keys match `deployment_cost_analysis.py`; ground truth scores only (plan Sec. 1, 5) | [`step1_groundtruth/verification.json`](step1_groundtruth/verification.json) — metric checks pass on real data; loader confirmed locally `(1000,200,512)`, `t_train_end=1.0` | verified |
| 2 | Stub harness + hello-hybrid smoke test (`coupling.py`, `trigger.py`, `_smoke.py`) | Stub-first / synthetic trigger (plan Sec. 6, 7); hard-switch hand-off (plan component 10); acceptance = hybrid between baselines (plan Sec. 9); closest prior work ANCHOR (arXiv:2512.19643) | [`step2_smoke/verification.json`](step2_smoke/verification.json) + [`step2_smoke/smoke_curves.png`](step2_smoke/smoke_curves.png) — `PASS`, switch at t≈1.005 | verified |
| 3 | Cost / latency profiler (`profiler.py`) | Warm-up + repeats + median, log-log slope; mirrors `deployment_cost_analysis.py` (REPEAT=30, WARMUP=5); target exponents ~1.0 from `cost_summary.json` | [`step3_profiler/verification.json`](step3_profiler/verification.json) + [`step3_profiler/scaling.png`](step3_profiler/scaling.png) — `timeit` sane; scaling fit recovers 1.5 exactly | verified |
| 4 | Accuracy-cost model (`accuracy_cost.py`) | Additive intervention-count cost (ANCHOR); diminishing-returns plateau (arXiv:2512.24776); per-model baselines (cost_summary.json) | [`step4_accuracy_cost/verification.json`](step4_accuracy_cost/verification.json) + [`step4_accuracy_cost/accuracy_cost.png`](step4_accuracy_cost/accuracy_cost.png) — cost additive; correction reduces error; target inversion meets target. Finding: late post-drift switch doesn't help → motivates M1/M2 | verified |
| 5 | Adaptive controller (`controller.py`) — **core** | Two-threshold hysteresis deadband prevents chattering (bang-bang control); correct only when trust low (plan Sec.12); fixed baseline (Step 3.2) | [`step5_controller/verification.json`](step5_controller/verification.json) + [`step5_controller/controller.png`](step5_controller/controller.png) — anti-chatter (1 vs 3 flips); tighter target→more correction (50→150); adaptive 0 vs fixed 16 corrections while trusted | verified |
| 6 | Deployment runtime (`runtime.py`) | `run(problem, accuracy_target) → (solution, cost report)` = plan contract 3 (Sec.8); single entry point (component 18) | [`step6_runtime/verification.json`](step6_runtime/verification.json) + [`step6_runtime/runtime_snapshot.png`](step6_runtime/runtime_snapshot.png) — accounting exact (100+100=200); switch@1.005; hybrid beats pure-ML; met target | verified |
| 7 | Pareto frontier (`pareto.py`) — **headline** | Report frontier + SLA, not a composite index (deployment_cost_writeup.md; arXiv:2512.24776); illustrative 10:1 cost ratio | [`step7_pareto/verification.json`](step7_pareto/verification.json) + [`step7_pareto/pareto.png`](step7_pareto/pareto.png) — knob traces trade-off; numerical-grade accuracy at ~44% lower cost; accurate branch beats pure-ML. Honest: strict domination not claimed (ideal 0-error stand-in) | verified |
| 8 | Robustness (`robustness.py`) | Target hit-rate on unseen/OOD + adaptive-vs-fixed ablation (plan Sec.7,10); 8 in-dist + 4 OOD ICs | [`step8_robustness/verification.json`](step8_robustness/verification.json) + [`pareto_robust.png`](step8_robustness/pareto_robust.png) + [`adaptive_vs_fixed.png`](step8_robustness/adaptive_vs_fixed.png) — variance across ICs; OOD harder; adaptive hit-rate 100% vs fixed dropping to 0%; adaptive 0.0 vs fixed 0.086 err at matched budget | verified |
| 9 | Integration (`integrate.py`, [guide](../../docs/integration/INTEGRATION.md)) | Integration by substitution behind fixed contracts (plan Sec.8); solver-agnostic engine | [`step9_integration/verification.json`](step9_integration/verification.json) + [`dry_run_frontier.png`](step9_integration/dry_run_frontier.png) — dry-run engine OK; **spectral adapter VERIFIED torch-free vs Cole–Hopf: 0.00036 rel-L2 (0.00006 extrap)**, works as per-step corrector; FNO adapter written (needs torch); M1/M2 hooks pending. Run `partial_main()` for real-solver frontier | solvers ready; partial run user-side |
| 10 | Live one-knob demo (`demo.py`, `app.py`) | Three curves + trust signal + one accuracy knob (plan Sec.9); `streamlit run .../app.py` | [`step10_demo/verification.json`](step10_demo/verification.json) + [`demo_preview.png`](step10_demo/demo_preview.png) — backend frame complete; knob changes outcome (89→103 corr, 0.013→0.0 err); cost report populated | verified (backend) |

Regression: `python -m hybrid_pde.control_214133E._verify_all` re-runs all step checks (14 assertions, all pass).

Each later step appends a row here and a folder with its `verification.json` (and a
plot where a figure adds insight).
